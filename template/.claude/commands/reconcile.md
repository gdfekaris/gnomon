---
description: Check the brain for drift and broken links (Task D)
---

Run **Standard Task D** from `AGENTS.md`, then report as a checklist.

Also verify, beyond what Task D specifies:
- every `[[slug]]` in every file resolves to a real file
- every dual-link has both forms (`[[slug]]` and a relative markdown link)
- every source directory has `raw.md`; note any missing `original.md` and
  whether that is correct for that source
- every `grounded-in:` slug in a principle exists
- any source still `curated: agent-proposed` (unratified)

Fix nothing. Report findings, and append anything needing a decision to
the proposals queue.
