Drop captures here as .md or .txt. Anything goes — a passage, a URL,
a half-thought. Then ask an agent to "process the inbox" (Task C in
AGENTS.md). Nothing here is deleted by agents; you clear it after review.
