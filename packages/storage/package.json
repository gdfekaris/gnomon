{
  "name": "@gnomon/storage",
  "version": "0.0.0",
  "private": true,
  "description": "Gnomon storage drivers: StorageDriver interface, GitHub (REST + GraphQL), Memory, Encrypting.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@gnomon/core": "0.0.0"
  }
}
